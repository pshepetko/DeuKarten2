import 'package:flutter/material.dart';

class AppColors {
  // Primary
  static const Color primary = Color(0xFF2F5BFF);
  static const Color primaryLight = Color(0xFFE8EDFF); // Light variant for backgrounds
  static const Color primaryDark = Color(0xFF1E3A9F);  // Dark variant

  // Secondary
  static const Color secondary = Color(0xFFFFCC00);
  static const Color secondaryLight = Color(0xFFFFF5CC);

  // Accent
  static const Color accent = Color(0xFFFF3B30);
  static const Color accentLight = Color(0xFFFFE5E5);

  // Backgrounds
  static const Color background = Color(0xFFF5F7FB);
  static const Color surface = Color(0xFFFFFFFF);  // Cards
  static const Color scaffoldBackground = Color(0xFFF5F7FB);

  // Text
  static const Color textPrimary = Color(0xFF1A1A1A);
  static const Color textSecondary = Color(0xFF6B7280);
  static const Color textTertiary = Color(0xFF9CA3AF);

  // Feedback
  static const Color success = Color(0xFF34C759);
  static const Color error = Color(0xFFFF3B30);
  static const Color warning = Color(0xFFFF9500);
  static const Color info = Color(0xFF2196F3);  // ← ДОДАНО

  // Gamification
  static const Color streak = Color(0xFFFF6B35);  // Fire orange
  static const Color xp = Color(0xFFFFCC00);      // Yellow gold
}
